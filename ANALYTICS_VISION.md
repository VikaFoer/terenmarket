# Виділення системи аналітики для Tecsa Market

## 📊 Мета
Створити комплексну систему відстеження метрик відвідуваності, поведінки користувачів та ефективності сайту для прийняття обґрунтованих бізнес-рішень.

---

## 🎯 Ключові метрики для відстеження

### 1. **Базові метрики відвідуваності**
- **Унікальні відвідувачі** (Unique Visitors)
- **Сесії** (Sessions) - кількість візитів
- **Перегляди сторінок** (Page Views)
- **Унікальні перегляди сторінок** (Unique Page Views)
- **Середня тривалість сесії** (Average Session Duration)
- **Показник відмов** (Bounce Rate)
- **Показник виходу** (Exit Rate)

### 2. **Глибина перегляду (Engagement Depth)**
- **Кількість переглянутих сторінок за сесію**
- **Час на сторінці** (Time on Page)
- **Глибина прокрутки** (Scroll Depth) - скільки % сторінки переглянуто
- **Взаємодія з елементами** (кліки, наведення, скрол)
- **Шлях користувача** (User Journey) - послідовність переглядів

### 3. **Метрики для QR-сторінок побажань**
- **Відвідуваність кожної QR-категорії** (/colorant, /mix, /bruker-o, /axs, /filter, /lab)
- **Кількість переглядів привітань** (скільки разів натиснуто "Ще одне побажання")
- **Кількість розгорнутих товарів** (скільки разів натиснуто "Розгорнути далі")
- **Конверсія email-реєстрацій** (скільки відвідувачів залишили email)
- **Час на QR-сторінці**
- **Джерела трафіку** (звідки прийшли на QR-сторінки)

### 4. **Метрики для клієнтської панелі**
- **Активні клієнти** (кількість унікальних входів)
- **Частота візитів клієнтів**
- **Найпопулярніші категорії** (які категорії переглядають найчастіше)
- **Найпопулярніші товари** (перегляди, додавання в кошик)
- **Метрики кошика**:
  - Кількість додавань в кошик
  - Розмір кошика (середня кількість товарів)
  - Відмови від кошика (додали, але не завершили)
- **Пошук товарів** (якщо буде додано)
- **Фільтрація по категоріях** (які категорії вибирають)

### 5. **Метрики для адмін-панелі**
- **Активність адмінів** (кількість входів, дій)
- **Операції з товарами** (додавання, редагування, видалення)
- **Операції з клієнтами** (додавання, редагування, призначення категорій)
- **Операції з категоріями** (CRUD операції)

### 6. **Технічні метрики**
- **Швидкість завантаження сторінок** (Page Load Time)
- **Помилки** (404, 500, JavaScript errors)
- **Пристрої та браузери** (Desktop/Mobile, Chrome/Firefox/Safari)
- **Географія** (країни, міста відвідувачів)

---

## 🏗️ Архітектура рішення

### Варіант 1: Гібридний підхід (Рекомендовано)
**Комбінація Google Analytics 4 + Власна база даних**

#### Переваги:
- ✅ Google Analytics 4 - безкоштовний, потужний інструмент з готовими звітами
- ✅ Власна БД - повний контроль над даними, можливість кастомної аналітики
- ✅ Відстеження авторизованих користувачів (GA4 має обмеження для приватних даних)
- ✅ GDPR compliance (можна контролювати зберігання даних)

#### Структура:
```
┌─────────────────┐
│  Frontend (React)│
│  - GA4 Tracking │
│  - Custom Events │
└────────┬─────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌──────────────┐   ┌──────────────────┐
│ Google GA4   │   │  Backend API     │
│ (Dashboard)  │   │  - Save events   │
│              │   │  - Analytics DB  │
└──────────────┘   └──────────────────┘
```

### Варіант 2: Тільки власна система
**Повна реалізація в нашій БД**

#### Переваги:
- ✅ Повний контроль
- ✅ Немає залежності від зовнішніх сервісів
- ✅ Можливість кастомної аналітики

#### Недоліки:
- ❌ Потрібно розробляти інтерфейс для перегляду метрик
- ❌ Більше часу на розробку

---

## 📋 План реалізації (Гібридний підхід)

### Етап 1: Базова інтеграція Google Analytics 4 (1-2 дні)

#### 1.1. Налаштування GA4
- Створити аккаунт Google Analytics 4
- Отримати Measurement ID (G-XXXXXXXXXX)
- Налаштувати цілі (Goals):
  - Email реєстрація на QR-сторінках
  - Вхід в систему (Login)
  - Додавання товару в кошик
  - Перегляд товарів

#### 1.2. Інтеграція в React
- Встановити `react-ga4` або `@analytics/google-analytics`
- Додати GA4 в `App.js` або окремий компонент `AnalyticsProvider`
- Налаштувати автоматичне відстеження переходів між сторінками
- Додати базові події (page_view, login, etc.)

#### 1.3. Відстеження подій
```javascript
// Приклади подій для відстеження
- page_view: Перегляд сторінки
- login: Вхід в систему
- view_item: Перегляд товару
- add_to_cart: Додавання в кошик
- email_signup: Реєстрація email
- greeting_view: Перегляд привітання
- expand_products: Розгортання списку товарів
```

### Етап 2: Власна система аналітики в БД (2-3 дні)

#### 2.1. Структура бази даних
```sql
-- Таблиця для зберігання подій
CREATE TABLE analytics_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  user_id INTEGER, -- NULL для неавторизованих
  event_type TEXT NOT NULL, -- 'page_view', 'click', 'scroll', etc.
  event_name TEXT NOT NULL, -- назва події
  page_path TEXT NOT NULL, -- шлях сторінки
  page_title TEXT,
  category TEXT, -- категорія події
  action TEXT, -- дія
  label TEXT, -- мітка
  value REAL, -- числове значення
  metadata TEXT, -- JSON з додатковими даними
  user_agent TEXT,
  ip_address TEXT,
  referrer TEXT,
  device_type TEXT, -- 'desktop', 'mobile', 'tablet'
  browser TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Таблиця для сесій
CREATE TABLE analytics_sessions (
  id TEXT PRIMARY KEY,
  user_id INTEGER,
  started_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  ended_at DATETIME,
  duration INTEGER, -- в секундах
  page_views INTEGER DEFAULT 0,
  device_type TEXT,
  browser TEXT,
  referrer TEXT,
  landing_page TEXT,
  exit_page TEXT
);

-- Таблиця для унікальних відвідувачів
CREATE TABLE analytics_visitors (
  id TEXT PRIMARY KEY, -- fingerprint або cookie ID
  first_visit DATETIME DEFAULT CURRENT_TIMESTAMP,
  last_visit DATETIME,
  total_visits INTEGER DEFAULT 1,
  total_page_views INTEGER DEFAULT 0,
  user_id INTEGER -- якщо зареєстрований
);

-- Індекси для швидкого пошуку
CREATE INDEX idx_events_session ON analytics_events(session_id);
CREATE INDEX idx_events_user ON analytics_events(user_id);
CREATE INDEX idx_events_type ON analytics_events(event_type);
CREATE INDEX idx_events_created ON analytics_events(created_at);
CREATE INDEX idx_sessions_user ON analytics_sessions(user_id);
CREATE INDEX idx_sessions_started ON analytics_sessions(started_at);
```

#### 2.2. Backend API для аналітики
```javascript
// server/routes/analytics.js

POST /api/analytics/track
- Зберігає подію в БД
- Створює/оновлює сесію
- Створює/оновлює відвідувача

GET /api/analytics/stats
- Загальна статистика (для адміна)
- Фільтри по датах, користувачах, подіях

GET /api/analytics/dashboard
- Дані для дашборду аналітики
- Популярні сторінки, події, користувачі
```

#### 2.3. Frontend компонент для відстеження
```javascript
// client/src/utils/analytics.js
- Функції для відправки подій
- Автоматичне відстеження переходів
- Відстеження скролу, кліків, часу на сторінці
```

### Етап 3: Розширене відстеження (2-3 дні)

#### 3.1. Відстеження глибини перегляду
- **Scroll Depth**: Відстеження прокрутки сторінки (0%, 25%, 50%, 75%, 100%)
- **Time on Page**: Час проведений на сторінці
- **Element Visibility**: Відстеження перегляду конкретних елементів
- **Click Tracking**: Відстеження кліків на важливі елементи

#### 3.2. Відстеження поведінки на QR-сторінках
- Кількість переглядів привітань
- Час на сторінці перед реєстрацією email
- Кількість розгорнутих товарів
- Конверсія email-реєстрацій

#### 3.3. Відстеження поведінки клієнтів
- Перегляди товарів по категоріях
- Додавання в кошик
- Видалення з кошика
- Фільтрація товарів

### Етап 4: Дашборд аналітики в адмін-панелі (3-4 дні)

#### 4.1. Компонент Analytics Dashboard
```javascript
// client/src/components/admin/AnalyticsDashboard.js

Розділи:
1. Загальна статистика
   - Унікальні відвідувачі
   - Сесії
   - Перегляди сторінок
   - Середня тривалість сесії
   - Показник відмов

2. QR-сторінки аналітика
   - Відвідуваність кожної категорії
   - Конверсія email-реєстрацій
   - Популярність привітань
   - Розгортання товарів

3. Клієнтська панель аналітика
   - Активні клієнти
   - Популярні категорії
   - Популярні товари
   - Метрики кошика

4. Графіки та візуалізація
   - Графік відвідуваності по днях
   - Графік конверсій
   - Теплова карта подій
   - User journey visualization

5. Експорт даних
   - Експорт в CSV
   - Експорт в Excel
   - API для інтеграції
```

#### 4.2. Бібліотеки для візуалізації
- `recharts` або `chart.js` для графіків
- `react-table` для таблиць з даними
- `date-fns` для роботи з датами

---

## 🔧 Технічні деталі реалізації

### 1. Встановлення залежностей

```bash
# Frontend
cd client
npm install react-ga4 date-fns recharts

# Backend (якщо потрібні додаткові бібліотеки)
cd server
npm install user-agent express-rate-limit
```

### 2. Структура файлів

```
client/src/
  ├── utils/
  │   └── analytics.js          # Утиліти для відстеження
  ├── components/
  │   └── analytics/
  │       ├── AnalyticsProvider.js  # Провайдер аналітики
  │       └── useAnalytics.js      # Hook для використання
  └── pages/
      └── admin/
          └── AnalyticsDashboard.js # Дашборд аналітики

server/
  ├── routes/
  │   └── analytics.js          # API маршрути
  ├── middleware/
  │   └── analytics.js           # Middleware для збору даних
  └── database.js                # Оновлення схеми БД
```

### 3. Приклад використання

```javascript
// В компоненті
import { useAnalytics } from '../components/analytics/useAnalytics';

const MyComponent = () => {
  const { trackEvent } = useAnalytics();
  
  const handleClick = () => {
    trackEvent('button_click', {
      button_name: 'expand_products',
      page: '/colorant'
    });
  };
  
  return <button onClick={handleClick}>Розгорнути</button>;
};
```

---

## 📈 Метрики для моніторингу

### Щоденні метрики:
- Унікальні відвідувачі
- Нові сесії
- Конверсії (email реєстрації, входів)

### Тижневі метрики:
- Топ-10 найпопулярніших сторінок
- Топ-10 найпопулярніших товарів
- Середня глибина перегляду
- Середній час на сайті

### Місячні метрики:
- Загальна статистика за місяць
- Тренди зростання/падіння
- Аналіз поведінки користувачів
- Оптимізація конверсій

---

## 🔒 Безпека та приватність

1. **GDPR Compliance**
   - Отримання згоди на збір даних (якщо потрібно)
   - Можливість видалення даних користувача
   - Анонімізація IP-адрес

2. **Захист даних**
   - Хешування чутливих даних
   - Обмеження доступу до аналітики (тільки адміни)
   - Rate limiting для API

3. **Анонімізація**
   - Не зберігати повні IP-адреси
   - Не зберігати паролі або токени
   - Використовувати session IDs замість особистих даних

---

## 🚀 Пріоритети реалізації

### Високий пріоритет (MVP):
1. ✅ Інтеграція Google Analytics 4
2. ✅ Базове відстеження подій (page_view, login, email_signup)
3. ✅ Таблиця analytics_events в БД
4. ✅ API для збереження подій
5. ✅ Базовий дашборд в адмін-панелі

### Середній пріоритет:
1. Відстеження глибини перегляду (scroll, time on page)
2. Детальна аналітика QR-сторінок
3. Метрики кошика
4. Графіки та візуалізація

### Низький пріоритет:
1. Теплові карти
2. User journey visualization
3. Прогнозна аналітика (ML)
4. A/B тестування

---

## 📊 Очікувані результати

Після реалізації системи аналітики ви зможете:

1. **Розуміти поведінку користувачів**
   - Які сторінки найпопулярніші
   - Як користувачі навігують по сайту
   - Де користувачі залишають сайт

2. **Оптимізувати конверсії**
   - Покращити QR-сторінки на основі даних
   - Збільшити кількість email-реєстрацій
   - Покращити UX на основі поведінки

3. **Приймати обґрунтовані рішення**
   - Які товари найпопулярніші
   - Які категорії потребують більше уваги
   - Де інвестувати маркетингові зусилля

4. **Моніторити здоров'я сайту**
   - Швидкість завантаження
   - Помилки та проблеми
   - Технічні метрики

---

## 🎯 Наступні кроки

1. **Обговорення та уточнення вимог**
   - Які метрики найважливіші?
   - Які звіти потрібні?
   - Як часто потрібні оновлення?

2. **Створення технічного завдання**
   - Детальний план реалізації
   - Оцінка часу та ресурсів
   - Пріоритизація задач

3. **Початок реалізації**
   - Етап 1: Базова інтеграція GA4
   - Етап 2: Власна система в БД
   - Етап 3: Дашборд

---

## 📝 Примітки

- Система аналітики повинна бути легковою та не впливати на продуктивність сайту
- Дані можна зберігати з затримкою (batch processing) для оптимізації
- Рекомендується встановити retention policy (наприклад, зберігати дані 1-2 роки)
- Можна додати real-time аналітику для критичних метрик

---

**Дата створення:** 2025-01-XX  
**Версія:** 1.0  
**Статус:** Draft для обговорення


