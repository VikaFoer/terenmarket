# Монолітний деплой на Railway - Один сервіс

## 🎯 Що змінилося

Тепер проект можна задеплоїти **одним сервісом** на Railway! Backend автоматично обслуговує frontend після збірки.

## 📋 Покрокова інструкція

### Крок 1: Створення проекту

1. Зайдіть на [railway.app](https://railway.app)
2. Натисніть **"New Project"**
3. Оберіть **"Deploy from GitHub repo"**
4. Виберіть репозиторій: `VikaFoer/smartmart`
5. Railway автоматично визначить проект

### Крок 2: Налаштування Root Directory

1. Відкрийте ваш сервіс
2. Settings → Source → Root Directory
3. **МАЄ БУТИ ПОРОЖНЄ** (не вказуйте нічого!)
4. Якщо там щось вказано - **видаліть**!

### Крок 3: Створення Volume (ОБОВ'ЯЗКОВО!)

Без Volume база даних буде втрачатися при кожному перезапуску.

1. У вашому проекті натисніть **"New"** → **"Volume"**
2. Налаштування:
   - **Name**: `database-storage` (або будь-яка назва)
   - **Mount Path**: `/app/server/data`
   - **Size**: 5 GB
3. Натисніть **"Add"**

### Крок 4: Змінні середовища

1. Settings → Variables
2. Додайте змінну:
   - **Name**: `DATA_DIR`
   - **Value**: `/app/server/data`
   - Натисніть **"Add"**

3. **Опціонально** (якщо потрібно кастомний API URL):
   - **Name**: `REACT_APP_API_URL`
   - **Value**: `/api` (за замовчуванням використовується відносний шлях)
   - Натисніть **"Add"**

### Крок 5: Railway автоматично зробить все інше

Railway автоматично:
1. Встановить залежності для client та server
2. Збудує React додаток (`npm run build`)
3. Запустить backend сервер, який обслуговує frontend

### Крок 6: Перевірка після деплою

1. Відкрийте **Deployments** → **View Logs**
2. Має бути:
   ```
   > cd client && npm install
   > cd server && npm install
   > cd client && npm run build
   > Compiled successfully!
   > cd server && NODE_ENV=production node index.js
   > Connected to SQLite database
   > Database initialized successfully
   > Server is running on port <PORT>
   ```

3. Відкрийте URL вашого сервісу
4. Має відкритися сторінка логіну SmartMarket
5. Спробуйте увійти:
   - **Логін**: `admin`
   - **Пароль**: `admin123`

## ✅ Переваги монолітного деплою

- ✅ **Один сервіс** замість двох
- ✅ **Простіше налаштування** - менше змінних середовища
- ✅ **Один URL** для всього додатку
- ✅ **Менше витрат** - один сервіс замість двох
- ✅ **Автоматичне обслуговування** frontend через backend

## 🔍 Як це працює

1. **Build фаза**: Railway збирає React додаток в `client/build`
2. **Start фаза**: Запускається Express сервер
3. **Production режим**: Express обслуговує статичні файли з `client/build`
4. **API запити**: Всі запити до `/api/*` обробляються Express routes
5. **React Router**: Всі інші запити перенаправляються на `index.html` (для React Router)

## 🐛 Розв'язання проблем

### Проблема: Frontend не відображається

**Перевірте:**
1. Логи показують "Compiled successfully!"
2. Логи показують "Server is running on port"
3. Перевірте чи `client/build` існує після build

**Рішення:**
- Переконайтеся що Root Directory порожнє
- Перевірте логи на помилки build
- Redeploy: Deployments → Redeploy

### Проблема: API запити не працюють

**Перевірте:**
1. Health endpoint: `https://your-app.railway.app/api/health`
2. Має повернути: `{"status":"ok"}`

**Рішення:**
- Перевірте логи на помилки
- Переконайтеся що база даних ініціалізується правильно

### Проблема: Дані втрачаються

**Причина**: Volume не створений

**Рішення:**
1. Створіть Volume з Mount Path `/app/server/data`
2. Переконайтеся що `DATA_DIR` = `/app/server/data`
3. Перезапустіть сервіс

## 📚 Порівняння з попередньою структурою

### Було (2 сервіси):
- Backend сервіс
- Frontend сервіс
- Потрібно налаштовувати `REACT_APP_API_URL`
- Два URL для моніторингу

### Стало (1 сервіс):
- Один монолітний сервіс
- Автоматичне обслуговування frontend
- Відносні шляхи для API (`/api`)
- Один URL для всього

## 💡 Важливі примітки

1. **Root Directory**: Має бути порожнє (корінь проекту)
2. **Volume**: Обов'язковий для збереження бази даних
3. **Змінна DATA_DIR**: Має відповідати Mount Path Volume
4. **REACT_APP_API_URL**: Не обов'язкова - за замовчуванням використовується `/api`
5. **NODE_ENV**: Автоматично встановлюється в `production` під час деплою

## 🎯 Чеклист

- [ ] Root Directory порожнє
- [ ] Volume створений з Mount Path `/app/server/data`
- [ ] Змінна `DATA_DIR` = `/app/server/data`
- [ ] Build пройшов успішно (логи показують "Compiled successfully!")
- [ ] Сторінка логіну відкривається
- [ ] Можна увійти як `admin` / `admin123`
- [ ] Всі функції працюють (товари, кошик, тощо)

