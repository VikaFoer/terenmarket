# Інструкція: Що робити після завантаження через Git

## 🎯 Простий покроковий план

### Крок 1: Підключитися до сервера

Відкрийте термінал (або використайте веб-консоль хостингу) та виконайте:

```bash
ssh tecsa@web41
```

### Крок 2: Перейти в директорію проєкту

```bash
cd /home/tecsa/tecsamarket.com.ua/www
```

### Крок 3: Зробити скрипт виконуваним та запустити

```bash
chmod +x setup-server.sh
./setup-server.sh
```

**Цей скрипт автоматично:**
- ✅ Встановить всі залежності
- ✅ Створить .env файл
- ✅ Збере фронтенд
- ✅ Створить потрібні директорії

### Крок 4: Відредагувати .env файл

Після того як скрипт завершиться, потрібно заповнити паролі:

```bash
cd server
nano .env
```

**Обов'язково змініть:**
- `JWT_SECRET` - будь-який довгий випадковий рядок (наприклад: `my-super-secret-key-12345`)
- `DB_PASSWORD` - пароль від вашої MySQL бази даних

**Як редагувати в nano:**
- Переміщатися: стрілки вгору/вниз
- Редагувати: просто друкуйте
- Зберегти: `Ctrl+O`, потім `Enter`
- Вийти: `Ctrl+X`

### Крок 5: Запустити сервер

```bash
# Простий запуск (для перевірки)
node index.js
```

Якщо бачите `Server is running on port 5000` - все працює! ✅

**Для запуску у фоні (щоб не закривався):**
```bash
nohup node index.js > app.log 2>&1 &
```

### Крок 6: Перевірити що працює

```bash
curl http://localhost:5000/api/health
```

Має повернути: `{"status":"ok"}`

## 🆘 Якщо щось не працює

### Помилка: "npm: command not found"
**Рішення:** Node.js не встановлений на сервері. Зверніться до підтримки хостингу.

### Помилка: "Cannot find module"
**Рішення:**
```bash
cd server
rm -rf node_modules
npm install
```

### Помилка: "Build directory not found"
**Рішення:**
```bash
cd client
npm run build
```

### Помилка: "Database connection failed"
**Рішення:** Перевірте `.env` файл - правильні дані для MySQL.

### Помилка: "Port 5000 already in use"
**Рішення:**
```bash
# Знайти процес
lsof -i :5000
# Зупинити (замініть <PID> на номер процесу)
kill -9 <PID>
```

## 📋 Швидкий чеклист

- [ ] Підключився до сервера (`ssh tecsa@web41`)
- [ ] Перейшов в директорію (`cd /home/tecsa/tecsamarket.com.ua/www`)
- [ ] Запустив `./setup-server.sh`
- [ ] Відредагував `.env` файл (заповнив паролі)
- [ ] Запустив сервер (`node index.js`)
- [ ] Перевірив що працює (`curl http://localhost:5000/api/health`)

## 🎉 Готово!

Якщо все працює локально, але сайт не відкривається в браузері - потрібно налаштувати Nginx (зверніться до підтримки хостингу).

