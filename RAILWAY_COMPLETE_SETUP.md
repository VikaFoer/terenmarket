# Повне налаштування SmartMarket на Railway

## 📋 Загальна інформація

Проект складається з **двох окремих сервісів**:
1. **Backend** (API Server) - Node.js + Express + SQLite
2. **Frontend** (React App) - React + Material-UI

## 🎯 Крок 1: Створення проекту на Railway

1. Зайдіть на [railway.app](https://railway.app)
2. Натисніть **"New Project"**
3. Оберіть **"Deploy from GitHub repo"**
4. Виберіть репозиторій: `VikaFoer/smartmart`
5. Railway автоматично створить перший сервіс (це буде Backend)

## 🔧 Крок 2: Налаштування Backend сервісу

### 2.1. Перевірка Root Directory

1. Відкрийте ваш Backend сервіс
2. Settings → Source → Root Directory
3. **МАЄ БУТИ ПОРОЖНЄ** (не вказуйте нічого!)
4. Якщо там щось вказано (наприклад, `/client/src/components`) - **видаліть**!

### 2.2. Створення Volume (ОБОВ'ЯЗКОВО!)

Без Volume база даних буде втрачатися при кожному перезапуску.

1. У вашому проекті натисніть **"New"** → **"Volume"**
2. Налаштування:
   - **Name**: `database-storage` (або будь-яка назва)
   - **Mount Path**: `/app/server/data`
   - **Size**: 5 GB
3. Натисніть **"Add"**

### 2.3. Змінні середовища для Backend

1. Відкрийте Backend сервіс → **Settings** → **Variables**
2. Додайте дві змінні:

   **Змінна 1:**
   - **Name**: `DATA_DIR`
   - **Value**: `/app/server/data`
   - Натисніть **"Add"**

   **Змінна 2:**
   - **Name**: `FRONTEND_URL`
   - **Value**: `https://your-frontend-url.railway.app` 
   - ⚠️ **Примітка**: Можна додати після деплою frontend, або залишити порожнім (буде використовуватись `http://localhost:3000`)

### 2.4. Перевірка Build налаштувань

Railway автоматично використає `nixpacks.toml` з кореня проекту.

**НЕ ПОТРІБНО** вручну встановлювати Build/Start команди!

### 2.5. Перевірка після деплою Backend

1. Відкрийте **Deployments** → **View Logs**
2. Має бути:
   ```
   > cd server && npm install
   > Connected to SQLite database
   > Database initialized successfully
   > Server is running on port <PORT>
   ```

3. Відкрийте URL вашого backend (наприклад, `https://smartmart-production.up.railway.app`)
4. Має показати JSON з інформацією про API
5. Перевірте health endpoint: `https://your-backend.railway.app/api/health`
   - Має повернути: `{"status":"ok"}`

## 🎨 Крок 3: Налаштування Frontend сервісу

### 3.1. Створення Frontend сервісу

1. У вашому Railway проекті натисніть **"New"** → **"Service"**
2. Оберіть **"GitHub Repo"** → `VikaFoer/smartmart`
3. Railway створить новий сервіс

### 3.2. Налаштування Root Directory для Frontend

1. Відкрийте Frontend сервіс
2. Settings → Source → Root Directory
3. **ВКАЖІТЬ**: `client`
4. ⚠️ **ВАЖЛИВО**: Для frontend має бути `client`, а не порожнє!

### 3.3. Змінна середовища для Frontend

1. Відкрийте Frontend сервіс → **Settings** → **Variables**
2. Додайте змінну:
   - **Name**: `REACT_APP_API_URL`
   - **Value**: `https://your-backend.railway.app/api`
   - ⚠️ **ВАЖЛИВО**: 
     - Замініть `your-backend.railway.app` на ваш реальний backend URL
     - URL має починатися з `https://`
     - URL має закінчуватися на `/api` (не `/api/`)
   - Натисніть **"Add"**

### 3.4. Перевірка Build налаштувань

Railway автоматично використає `client/nixpacks.toml`, який:
- Встановить залежності: `npm install`
- Збудує проект: `npm run build`
- Запустить сервер: `npx serve -s build -l $PORT`

**НЕ ПОТРІБНО** вручну встановлювати Build/Start команди!

### 3.5. Перевірка після деплою Frontend

1. Відкрийте **Deployments** → **View Logs**
2. Має бути:
   ```
   > npm install
   > npm run build
   > Compiled successfully!
   > npx serve -s build -l <PORT>
   ```

3. Відкрийте URL вашого frontend
4. Має відкритися сторінка логіну
5. Спробуйте увійти:
   - **Логін**: `admin`
   - **Пароль**: `admin123`

## ✅ Фінальна перевірка

### Backend:
- [ ] Root Directory порожнє
- [ ] Volume створений з Mount Path `/app/server/data`
- [ ] Змінна `DATA_DIR` = `/app/server/data`
- [ ] Змінна `FRONTEND_URL` = `https://your-frontend.railway.app` (опціонально)
- [ ] Health endpoint працює: `/api/health` → `{"status":"ok"}`
- [ ] Можна увійти: `admin` / `admin123`

### Frontend:
- [ ] Root Directory = `client`
- [ ] Змінна `REACT_APP_API_URL` = `https://your-backend.railway.app/api`
- [ ] Сторінка логіну відкривається
- [ ] Можна увійти як адміністратор
- [ ] Всі функції працюють (товари, кошик, тощо)

## 🔍 Структура після правильного налаштування

```
Railway Project: smartmart
│
├── smartmart (Backend Service) ✅
│   ├── Root Directory: (порожнє)
│   ├── Variables:
│   │   ├── DATA_DIR = /app/server/data
│   │   └── FRONTEND_URL = https://smartmart-frontend.up.railway.app
│   ├── Volume: database-storage
│   │   └── Mount Path: /app/server/data
│   └── URL: https://smartmart-production.up.railway.app
│
└── smartmart-frontend (Frontend Service) ✅
    ├── Root Directory: client
    ├── Variables:
    │   └── REACT_APP_API_URL = https://smartmart-production.up.railway.app/api
    └── URL: https://smartmart-frontend.up.railway.app
```

## 🐛 Розв'язання проблем

### Проблема: Backend не запускається

**Перевірте:**
1. Root Directory має бути порожнє
2. Volume має бути створений
3. Змінна `DATA_DIR` має бути встановлена
4. Перевірте логи на помилки

**Рішення:**
- Видаліть всі налаштування Build/Start в Settings
- Переконайтеся що Root Directory порожнє
- Redeploy: Deployments → Redeploy

### Проблема: Frontend не підключається до Backend

**Перевірте:**
1. Змінна `REACT_APP_API_URL` має бути правильна
2. Backend має працювати (перевірте health endpoint)
3. DevTools → Console на помилки CORS

**Рішення:**
- Перевірте чи backend URL правильний
- Переконайтеся що URL починається з `https://` і закінчується на `/api`
- Перезапустіть frontend після зміни змінної

### Проблема: Дані втрачаються

**Причина**: Volume не створений або не підключений

**Рішення:**
1. Створіть Volume з Mount Path `/app/server/data`
2. Переконайтеся що `DATA_DIR` = `/app/server/data`
3. Перезапустіть backend

## 📚 Додаткові ресурси

- **Детальний гайд**: `RAILWAY_DEPLOYMENT_GUIDE.md`
- **Швидкий чеклист**: `RAILWAY_CHECKLIST.md`
- **Frontend налаштування**: `FRONTEND_RAILWAY_SETUP.md`
- **База даних**: `DATABASE_SETUP.md`

## 💡 Важливі примітки

1. **Root Directory**:
   - Backend: **порожнє**
   - Frontend: **`client`**

2. **Змінні середовища**:
   - Backend: `DATA_DIR`, `FRONTEND_URL`
   - Frontend: `REACT_APP_API_URL`

3. **Volume**:
   - Обов'язковий для Backend
   - Mount Path: `/app/server/data`
   - Змінна `DATA_DIR` має відповідати Mount Path

4. **Після зміни змінних**:
   - Railway автоматично перезапустить сервіс
   - Зачекайте поки деплой завершиться

5. **Custom Domain** (опціонально):
   - Можна налаштувати власний домен для кожного сервісу
   - Settings → Domains → Add Domain

