# Чеклист для деплою на Railway

## ✅ Обов'язкові кроки

### Backend налаштування:

- [ ] **Root Directory**: ПОРОЖНЄ (не `/client/src/components`, не `/server`)
- [ ] **Volume створений**: Name `database-storage`, Mount Path `/app/server/data`
- [ ] **Змінна DATA_DIR**: Значення `/app/server/data`
- [ ] **Змінна FRONTEND_URL**: Значення `https://your-frontend.railway.app` (після деплою frontend)
- [ ] **Build Command**: Залишити порожнім (Railway використає nixpacks.toml)
- [ ] **Start Command**: Залишити порожнім (Railway використає nixpacks.toml)

### Перевірка після деплою:

- [ ] Логи показують: `Connected to SQLite database`
- [ ] Логи показують: `Database initialized successfully`
- [ ] Логи показують: `Server is running on port <PORT>`
- [ ] Health endpoint працює: `/api/health` → `{"status":"ok"}`
- [ ] Можна увійти: `admin` / `admin123`

## 🔍 Якщо щось не працює

### 1. Перевірте Root Directory
```
Settings → Source → Root Directory
Має бути ПОРОЖНЄ або не вказано взагалі
```

### 2. Перевірте Volume
```
Volumes → має бути database-storage
Mount Path: /app/server/data
```

### 3. Перевірте Variables
```
Settings → Variables
DATA_DIR = /app/server/data
```

### 4. Перевірте логи
```
Deployments → View Logs
Шукайте помилки або попередження
```

## 📝 Швидке виправлення

Якщо все ще не працює:

1. **Видаліть всі налаштування** в Settings → Build
2. **Переконайтеся** що Root Directory порожнє
3. **Переконайтеся** що Volume створений
4. **Redeploy**: Deployments → Redeploy

## 🎯 Очікуваний результат

Після правильного налаштування:
- ✅ API відповідає на запити
- ✅ База даних створюється автоматично
- ✅ Адміністратор доступний (admin/admin123)
- ✅ Всі endpoints працюють

